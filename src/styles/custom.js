// ─── Re-export tokens as colors for backward compatibility ───
// All styles are now in style.js — this file exists only for
// `import { colors }` used across existing components.
export { tokens as colors } from './style';
